
public abstract class Obstacle : ItemMap {
    /// <summary>
    /// Classe para definir objetos que não podem ser ultrapassados
    /// </summary>
    /// <param name="Symbol"></param>
    /// <returns></returns>

    public Obstacle(string Symbol) : base(Symbol) {}

}